import React from 'react';

function Reservations() {
  return (
    <main id="main" className="reservations-page">
      <section>
        <h1>Reservations</h1>
        <p>Use the Reserve a Table page to book a table with us.</p>
      </section>
    </main>
  );
}

export default Reservations;
